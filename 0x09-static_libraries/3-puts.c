#include "main.h"

/**
 * _puts - prints a string followed by a new line
 * @str: string to be printed
 * Return: always 0 (success)
 */

void _puts(char *str)
{
	int i;

	for (i = 0 ; i[str] != '\0' ; i++)
	{
		_putchar(i[str]);
	}
	_putchar('\n');
}

